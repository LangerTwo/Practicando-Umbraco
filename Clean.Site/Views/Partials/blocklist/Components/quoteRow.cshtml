@inherits UmbracoViewPage<BlockListItem>

@{
    var row = (QuoteRow)Model.Content;
    var settings = (QuoteRowSettings)Model.Settings;
    if(settings?.Hide ?? false) {return;}
}

<div class="row gx-4 gx-lg-5 justify-content-center bg-light text-end">
    <figure>
        <blockquote cite="@(row.CiteLink != null ? row.CiteLink : null)">
            @row.QuoteText
        </blockquote>
        @if(!string.IsNullOrWhiteSpace(row.QuoteAuthor)){
            <figcaption>-@row.QuoteAuthor
                @if(!string.IsNullOrWhiteSpace(row.AuthorJobTitle)){
                    <cite>
                        @row.AuthorJobTitle
                    </cite>
                } 
            </figcaption>
        }
    </figure>
</div>